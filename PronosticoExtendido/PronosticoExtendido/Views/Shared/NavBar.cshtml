<!--NavBar Superior-->
<nav class="navbar navbar-expand-lg navbar-dark bg-orange px-5">

    <!-- Logo -->
    <a class="navbar-brand p-0" href="#"><img src="/assets/img/logo.png" /></a>

    <!-- Collapsibe Button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsibe Panel -->
    <div class="navbar-collapse collapse">
        <ul class="nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="btn btn-sm bg-orange border border-white px-4" href="#" id="navbardrop" data-toggle="dropdown">INICIAR SESION <i class="fa fa-user pl-2" aria-hidden="true"></i></a>
                    <div class="dropdown-menu" style="width:350px; padding:30px; font-size:10px; margin-left:-180px">
                        <form name="form" class="css-form" novalidate>
                            <label for="">Usuario</label>
                            <input type="text" name="user" ng-model="user.name" class="form-control" required />
                            <div ng-show="form.$submitted || form.user.$touched">
                                <div ng-show="form.user.$error.required">El nombre de usuario es requerido</div>
                            </div>
                            <label for="">Contraseña</label>
                            <input type="text" ng-model="user.password" name="password" class="form-control" required />
                            <div ng-show="form.$submitted || form.password.$touched">
                                <div ng-show="form.password.$error.required">El nombre de usuario es requerido</div>
                            </div>
                            <button type="button" ng-click="form.$valid && Login(user)" class="btn btn-block bg-orange mt-5">INICIAR SESION</button>
                        </form>
                    </div>
                </li>
        </ul>
        </div>
</nav>